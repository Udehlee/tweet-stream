syntax = "proto3";

package grpc;

option go_package = "github.com/Udehlee/tweet-stream/pb";

import "tweet.proto";

message Empty {}

service TweetService {
  rpc StreamTweets(Empty) returns (stream tweet.Tweet);
}
